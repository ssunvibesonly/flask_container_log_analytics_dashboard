# 아키텍처 설계 문서

이 문서는 로그 분석 대시보드 백엔드의 설계 결정 사항에 대해 설명합니다.

## 핵심 원칙

- **관심사 분리 (Separation of Concerns)**: 각 컴포넌트(`producer`, `collector`, `api`)는 단 하나의 명확하게 정의된 책임을 가집니다.
- **단순성 (Simplicity)**: ELK/Fluentd와 같은 무거운 프레임워크를 피하고, 핵심 개념과 학습 친화적인 아키텍처에 집중합니다.
- **명확한 계약 (Clear Contracts)**: API 계약은 백엔드와 프론트엔드 간의 소통을 위한 신뢰의 원천(Source of Truth)입니다.

## 컴포넌트 상세 설명

### 1. Docker Compose 오케스트레이션

다중 컨테이너 환경(`db`, `api`, `producer`, `collector`)을 관리하기 위해 Docker Compose를 사용합니다.

- **선택 이유**: 단일 명령어(`docker-compose up`)로 모든 서비스를 정의, 빌드, 실행할 수 있습니다. 이는 모든 팀원에게 일관된 개발 환경을 보장하고 배포를 단순화합니다.

### 2. 데이터 파이프라인 (`producer` -> `collector`)

- **Producer**: 로그 파일을 한 줄씩 읽어 `stdout`(표준 출력)으로 출력하는 간단한 파이썬 스크립트입니다.
- **Collector**: `docker-py` 라이브러리를 사용하여 `producer`의 로그 스트림(`docker logs -f`와 동일)에 연결되는 또 다른 파이썬 스크립트입니다. 각 라인을 파싱하여 PostgreSQL 데이터베이스에 대량 삽입(bulk insert)합니다.
- **이 접근 방식을 선택한 이유**: 이 방식은 로그 생성과 로그 수집을 분리(decouple)합니다. `stdout`을 인터페이스로 사용함으로써 특정 기술에 종속되지 않습니다. `collector`를 변경하지 않고도 `producer`를 실제 Nginx 컨테이너로 쉽게 교체할 수 있습니다.

### 3. Flask API (`log_analytics_api`)

API는 유지보수와 테스트가 용이하도록 현대적인 Flask 패턴을 사용하여 구축되었습니다.

- **앱 팩토리 (`create_app`)**: 애플리케이션이 함수 내에서 생성됩니다.
    - **선택 이유**: 테스트 환경을 위해 다른 설정(예: 테스트 데이터베이스)을 가진 별도의 앱 인스턴스를 만들 수 있으므로 테스트에 매우 중요합니다. 또한 순환 참조(circular dependency) 문제를 방지하는 데 도움이 됩니다.
- **블루프린트 (Blueprints)**: API 엔드포인트는 "블루프린트" 단위로 구성됩니다.
    - **선택 이유**: 모든 라우트를 하나의 큰 파일에 정의하는 대신, 관련된 라우트들을 그룹화합니다(예: 모든 통계 관련 라우트는 `api/stats.py`에). 이는 프로젝트가 커져도 구조를 체계적으로 유지하는 데 도움이 됩니다.
- **서비스 계층 (`services/`)**: 실제 비즈니스 로직이 API 라우트 핸들러와 분리됩니다.
    - **선택 이유**: 블루프린트의 라우트 핸들러는 HTTP 요청과 응답을 처리하는 책임만 가져야 합니다. 데이터베이스 쿼리, Pandas를 이용한 데이터 집계와 같은 복잡한 로직은 서비스 계층에 위치합니다. 이를 통해 로직을 재사용하고 웹 컨텍스트와 독립적으로 유닛 테스트하기가 훨씬 쉬워집니다.
